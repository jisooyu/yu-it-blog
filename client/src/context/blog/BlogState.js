import React, { useReducer } from 'react';
import BlogContext from './blogContext';
import blogReducer from './blogReducer';

import { ADD_BLOGS, UPDATE_BLOGS } from '../../types.js';

const BlogState = () => {
  return <div></div>;
};

export default BlogState;
